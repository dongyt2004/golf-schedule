'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
let _moment;
const _getMoment = () => {
    _moment = _moment || require('moment');
    return _moment;
};
const _getTiming = (date) => {
    return {
        years: [date.get('year')],
        months: [date.get('month') + 1],
        days: [date.get('date')],
        hours: [date.get('hour')],
        minutes: [date.get('minute')],
    };
};
exports.getFutureTiming = (date) => {
    const moment = _getMoment();
    if (!moment.isMoment(date)) {
        date = moment(date);
    }
    return _getTiming(date);
};
exports.getFutureUtcTiming = (date) => {
    const moment = _getMoment();
    if (!moment.isMoment(date)) {
        date = moment.utc(date);
    }
    return _getTiming(date.utc());
};
//# sourceMappingURL=timingUtils.js.map