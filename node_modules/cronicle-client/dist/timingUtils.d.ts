import { Moment } from 'moment';
import { ITiming } from './dataTypes';
export declare const getFutureTiming: (date: string | Date | Moment) => ITiming;
export declare const getFutureUtcTiming: (date: string | Date | Moment) => ITiming;
